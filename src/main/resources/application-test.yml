spring:
  datasource:
    url: jdbc:h2:mem:dostoread-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  minio:
    endpoint: http://localhost:9000
    access-key: test
    secret-key: test
